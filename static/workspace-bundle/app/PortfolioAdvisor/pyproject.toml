[project]
name = "portfolio-advisor"
version = "0.1.0"
description = "Portfolio Advisor Agent — Capital Markets"
requires-python = ">=3.13"
dependencies = [
    "strands-agents>=0.1.0",
    "strands-agents-tools>=0.1.0",
    "boto3>=1.35.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
